
1. 객체지향 mcfunction 제작

-> 프로젝트 이름이 ProjectName인 경우

class Backpack():
    List itemList
    int currentIndex
    int size

    public Backpack():
        this.itemList = new List()
        this.currentIndex = -1
        this.size = 0
    
    def add(ItemNBT item) -> void:
        itemList.append(item)




/*
#위는 다음과 같이 컴파일됨.
class <ProjectName>/Backpack extends MCObject

    #MCObject/NBT/List itemList
    #int currentIndex
    #int size

    function <ProjectName>/Backpack/init:
        set heapSpaceTemp {id:-1,itemList:[],currentIndex:-1,size:0,class:"backpack"}
        set heapSpaceTemp.id from ObjectID
        inc ObjectID
        append heapSpace heapSpaceTemp

    function <ProjectName>/Backpack/add:
        append itemList stack0
*/



class EcomonyPlayer(Player):
    int playerID
    int vault
    Backpack backpack
    
    public EcomonyPlayer(int id, int vault):
        this.id = id
        this.vault = vault
        this.backpack = new Backpack()
    
    def setVault(int v) -> void:
        this.vault = v
    
    def getVault() -> int:
        return this.vault

    def getBackpackItem(int index):
        ...





class <ProjectName>:
    #이건 없어도 됨. 근데 있으면 tick.mcfunction으로써 컴파일.
    def tick() -> void:
        main()

    def main() -> void:
        execute as @a[instanceof =! EcomonyPlayer] :
            #@s는 self로 다시 쓰임
            #모든 엔티티는 .is(MCObject obj)라는 메소드를 가지는데, 이는 이 엔티티가 다음의 객체로
            #취급될 수 있음을 의미함. 이는 int자료형은 모두 scoreboard로 할당하라는 것과 마찬가지.
            self.is(new EcomonyPlayer(self.nbt.uuid[0], 0))





    
       